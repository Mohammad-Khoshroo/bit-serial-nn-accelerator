# مقدمه

در بسیاری از سامانه‌های هوش مصنوعی، شتاب‌دهنده‌های سخت‌افزاری تنها بخشی از کل سیستم هستند و بخش عمده منطق اجرای شبکه عصبی در نرم‌افزار انجام می‌شود. در چنین شرایطی لازم است نرم‌افزار بتواند با سخت‌افزار ارتباط برقرار کرده، داده‌ها را ارسال کند، عملیات را آغاز کند و نتایج را دریافت نماید.

در گام نخست لازم است با استفاده از کتابخانه‌های **Cocotb** و **cocotbext-axi**، لایه راه‌انداز نرم‌افزاری ایجاد کنید که بتوان از داخل برنامه‌های پایتون، شتاب‌دهنده پایه را مانند یک تابع معمولی فراخوانی کرد.

همچنین در این پروژه می‌بایست عملیات نگاشت و زمان‌بندی دو لایه **Conv2d** و **Linear** را به‌صورت نرم‌افزاری بر روی هسته انجام داده و با استفاده از راه‌انداز پیاده‌سازی‌شده بر روی هسته شبیه‌سازی کرده و در نهایت خروجی نهایی را با خروجی مدل طلایی مقایسه کنید.

[توضیح: منظور از راه‌انداز نرم‌افزاری یا Driver، بخشی از کد پایتون است که ارسال داده، ارسال وزن‌ها، تنظیم رجیسترهای کنترلی، شروع عملیات و دریافت خروجی از سخت‌افزار را مدیریت می‌کند.]

[توضیح: نگاشت یا Mapping یعنی تبدیل محاسبات یک لایه شبکه عصبی به مجموعه‌ای از عملیات قابل اجرا روی سخت‌افزار. زمان‌بندی یا Scheduling یعنی تعیین ترتیب اجرای این عملیات، ارسال داده‌ها و دریافت خروجی‌ها.]

[توضیح: مدل طلایی یا Golden Model معمولاً همان پیاده‌سازی مرجع نرم‌افزاری، مثلاً در PyTorch یا NumPy، است که خروجی سخت‌افزار با آن مقایسه می‌شود.]

در گام دوم این تمرین، شما با یک معماری پایه از یک شتاب‌دهنده شبکه عصبی آشنا می‌شوید و سپس با اعمال تکنیک‌های بهینه‌سازی در سطح معماری، آن را از حالت پردازش موازی **Bit-Parallel** به پردازش بیت‌سریال **Bit-Serial** ارتقا خواهید داد.

در نهایت، با بهره‌گیری از تنکی داده‌ها در سطح بیت **Bit-level Sparsity**، محاسبات بی‌اثر را حذف کرده و کارایی سیستم را بهبود می‌بخشید. همچنین با استفاده از محیط شبیه‌سازی بخش قبل، درستی‌سنجی انجام داده و نتایج را مقایسه کنید.

[توضیح: Bit-level Sparsity یعنی در نمایش دودویی داده‌ها، بسیاری از بیت‌ها صفر هستند. در محاسبات بیت‌سریال می‌توان از این صفر بودن بیت‌ها استفاده کرد و برخی عملیات‌ها را انجام نداد.]

---

# گام اول: شبیه‌سازی و درستی‌سنجی سیستم با Cocotb

## آشنایی با Cocotb

ابزار **Cocotb** که مخفف **Coroutine-based Co-simulation Testbench** است، یک فریم‌ورک متن‌باز برای نوشتن **Testbench** به زبان **Python** است. برخلاف Testbenchهای سنتی که با Verilog یا VHDL نوشته می‌شوند، در Cocotb کنترل کامل شبیه‌سازی در اختیار برنامه Python قرار می‌گیرد.

مزایای این روش عبارت‌اند از:

- تولید آسان داده‌های تست
- تحلیل نتایج با امکانات Python
- امکان مدل‌سازی سیستم‌های سطح بالا

برای اطلاعات بیشتر به سایت زیر مراجعه فرمایید:

https://www.cocotb.org

[توضیح: Cocotb اجازه می‌دهد به‌جای نوشتن تست‌بنچ‌های پیچیده در Verilog/VHDL، رفتار تست را با Python بنویسید و از امکاناتی مثل NumPy، PyTorch، فایل‌خوانی، تحلیل عددی و مقایسه خروجی‌ها استفاده کنید.]

---

## کتابخانه cocotbext-axi

نوشتن تراکنش‌های **AXI** به‌صورت دستی بسیار زمان‌بر است. به همین دلیل از کتابخانه **cocotbext-axi** استفاده می‌شود. این کتابخانه یک **AXI Master** کامل در اختیار برنامه قرار می‌دهد.

برای اطلاعات بیشتر و مشاهده کدهای نمونه به آدرس زیر مراجعه فرمایید:

https://github.com/alexforencich/cocotbext-axi

[توضیح: در این پروژه، cocotbext-axi نقش تولیدکننده تراکنش‌های AXI را دارد؛ یعنی به کمک آن می‌توان از داخل Python روی باس AXI نوشت، از آن خواند و رفتار Host را شبیه‌سازی کرد.]

---

## ساختار واسط‌ها

ماژول سخت‌افزاری دارای سه واسط AXI مستقل است.

### واسط Configuration

- ارسال تنظیمات
- شروع عملیات
- خواندن وضعیت پایان اجرا

[توضیح: این واسط معمولاً برای نوشتن رجیسترهای کنترلی مثل اندازه لایه، تعداد PEها، آدرس‌ها، سیگنال start و خواندن سیگنال done استفاده می‌شود.]

### واسط Input & Weight

- ارسال ورودی‌ها
- ارسال وزن‌های لایه

[توضیح: این واسط برای پر کردن حافظه‌های داخلی سخت‌افزار، مانند BRAM ورودی و BRAM وزن‌ها، استفاده می‌شود.]

### واسط Result

- ارسال Bias
- دریافت خروجی محاسبه‌شده

[توضیح: در این پروژه ظاهراً Bias از مسیر Result ارسال می‌شود و خروجی نهایی نیز از همین واسط خوانده می‌شود.]

---

## خروجی مورد انتظار

همراه با صورت پروژه، فایل اولیه کدهای شبیه‌ساز در اختیار شما قرار داده می‌شود. در پایان پروژه انتظار می‌رود بتوانید:

- یک راه‌انداز سخت‌افزاری مبتنی بر Cocotb طراحی کنید.
- ارتباط سه واسط AXI را مدیریت نمایید.
- ورودی و وزن لایه ورودی را چندی‌سازی کنید.
- لایه ورودی را نگاشت و زمان‌بندی کنید.
- ورودی هر بخش را برای سخت‌افزار ارسال کنید.
- پایان اجرای سخت‌افزار را تشخیص دهید.
- خروجی را دریافت کرده و به واحد زمان‌بند بازگردانید.
- خروجی نهایی را **DeQuantize** کنید.
- خروجی لایه شبیه‌سازی‌شده را با مدل طلایی مقایسه کنید.

[توضیح: منظور از DeQuantize تبدیل خروجی کوانتایز‌شده، مثلاً عدد صحیح ۸ بیتی یا ۳۲ بیتی، به مقدار واقعی یا اعشاری متناظر آن است تا بتوان آن را با خروجی مدل طلایی مقایسه کرد.]

نکته: برای پیاده‌سازی بخش‌های زمان‌بند و راه‌انداز می‌توانید الگوریتم‌های پیاده‌سازی‌شده در کدهای C را ملاحظه کرده و مشابه آن‌ها عمل کنید.

همچنین از طریق این لینک می‌توانید به پروژه دسترسی داشته باشید.

https://github.com/EAIoTIR/EAIoT-BackEnd

---

# گام دوم: معماری سخت‌افزاری شتاب‌دهنده

## آشنایی با پلتفرم سخت‌افزاری فعلی، معماری Bit-Parallel

پلتفرم فعلی، یک هسته پردازشی ساده برای محاسبات لایه‌های شبکه‌های عصبی، مانند **Fully Connected**، است که بر پایه جریان داده **Output Stationary** طراحی شده است.

در این معماری، هر واحد پردازشی **PE** وظیفه محاسبه و انباشت مقدار نهایی یک خروجی را بر عهده دارد. تمامی ارتباطات سیستم با پردازنده میزبان **Host** از طریق پروتکل **AXI4** صورت می‌گیرد.

[توضیح: در جریان داده Output Stationary، مقدار خروجی یا جمع جزئی خروجی داخل PE در یک رجیستر ثابت نگه داشته می‌شود و ورودی‌ها و وزن‌ها روی آن اعمال می‌شوند تا مقدار نهایی خروجی ساخته شود.]

---

## اجزای اصلی معماری پایه

### حافظه‌های داده، Input/Weight BRAMs

این ماژول شامل حافظه‌های **BRAM** است که از طریق باس **AXI** مقداردهی می‌شوند. وزن‌ها به‌صورت بلوک‌های مجزا برای هر **PE** ذخیره شده و داده‌های ورودی در یک بلاک مشترک قرار دارند. همچنین با پیکربندی سیستم می‌توان تعداد این واحدهای محاسباتی را تعیین نمود.

[توضیح: BRAM حافظه داخلی روی FPGA یا ماژول حافظه بلوکی در طراحی سخت‌افزار است که برای ذخیره ورودی‌ها، وزن‌ها و گاهی خروجی‌ها استفاده می‌شود.]

### مسیر داده واحد Cluster

در هر سیکل کلاک، یک داده ورودی ۸ بیتی از حافظه خوانده شده و به‌طور همزمان به تمامی واحدهای پردازشی ارسال یا **Broadcast** می‌شود. عملیات **MAC** یا **Multiply-and-Accumulate** داخل هر واحد پردازشی توسط یک ضرب‌کننده و یک جمع‌کننده انجام می‌گیرد.

داده ورودی مشترک در همان سیکل در وزن ۸ بیتی اختصاصی همان **PE** ضرب شده و نتیجه با مقدار ۳۲ بیتی انباشت‌گر **Accumulator** جمع می‌شود.

[توضیح: عملیات MAC یعنی ضرب ورودی در وزن و جمع کردن حاصل ضرب با مقدار قبلی انباشت‌گر.]

### کنترلر واحد Cluster

این واحد شامل یک ماشین حالت بوده که اجزای سیستم را هماهنگ کرده و سیگنال‌های کنترلی لازم را برای واحدهای محاسباتی و حافظه‌های داده صادر می‌کند.

[توضیح: ماشین حالت یا FSM تعیین می‌کند سخت‌افزار در هر لحظه در چه مرحله‌ای است؛ مثلاً بارگذاری ورودی، اجرای محاسبه، افزایش آدرس، پایان محاسبه و ارسال done.]

---

# مقدمه‌ای بر محاسبات بیت‌سریال، Bit-Serial Computation

در پیاده‌سازی سخت‌افزاری شبکه‌های عصبی عمیق **DNNs**، نحوه نمایش و پردازش عملوندها تأثیر مستقیمی بر مساحت تراشه، توان مصرفی و زمان اجرا دارد. در معماری فعلی، یعنی معماری موازی، تمام بیت‌های یک عملوند ۸ بیتی در یک سیکل کلاک وارد ضرب‌کننده می‌شوند.

در محاسبات بیت‌سریال، عملوندها به‌جای پردازش موازی، بیت‌به‌بیت و طی چند سیکل متوالی پردازش می‌شوند. در شبکه‌های عصبی، معمولاً یک عملوند، مثلاً وزن، به‌صورت موازی **Parallel** ثابت نگه داشته می‌شود و عملوند دیگر، مثلاً ورودی، به‌صورت سریال شیفت داده می‌شود.

در هر سیکل، تنها یک بیت از ورودی بررسی می‌شود:

- اگر بیت ورودی یک باشد، مقدار وزن، با در نظر گرفتن ارزش مکانی بیت ورودی، یعنی مقدار **Shift**، شیفت داده شده و با مقدار انباشت‌گر جمع می‌شود.
- اگر بیت ورودی صفر باشد، عملیات جمع صورت نمی‌گیرد و به سراغ بیت بعدی ورودی می‌رویم.

[توضیح: برای مثال اگر بیت شماره ۳ ورودی برابر ۱ باشد، وزن باید به اندازه ۳ بیت شیفت داده شود؛ یعنی مقدار وزن در ۲ به توان ۳ ضرب می‌شود.]

---

## مزایای محاسبات Bit-Serial

از جمله مزایای محاسبات **Bit-Serial** می‌توان به موارد زیر اشاره نمود:

### ۱. کاهش مساحت و توان

جایگزینی ضرب‌کننده‌های کامل، که نیازمند گیت‌های منطقی زیادی هستند، با گیت‌های **AND**، شیفت‌رجیستر و جمع‌کننده.

[توضیح: ضرب‌کننده سخت‌افزاری کامل معمولاً پرهزینه‌تر از مجموعه‌ای از شیفت و جمع است، مخصوصاً وقتی هدف کاهش توان یا مساحت باشد.]

### ۲. بهره‌وری از تنکی داده‌ها در سطح بیت

در حالی که بیشتر شتاب‌دهنده‌های شبکه عصبی قادر به حذف محاسبات غیرضرور در صورت صفر بودن کامل یک مقدار ورودی هستند، یعنی **Value-level sparsity**، با استفاده از محاسبات سریال می‌توان حتی از صفر بودن بیت‌ها در اپرندهای غیرصفر نیز برای افزایش بهره‌وری استفاده نمود.

[توضیح: Value-level sparsity یعنی کل مقدار ورودی صفر باشد. Bit-level sparsity یعنی مقدار ورودی لزوماً صفر نیست، اما تعدادی از بیت‌های نمایش دودویی آن صفر هستند.]

---

## معایب محاسبات Bit-Serial

از طرفی این روش معایب زیر را می‌تواند به همراه داشته باشد:

### ۱. افزایش تأخیر، Latency

پردازش ضرب دو عدد ۸ بیتی، اکنون به‌جای ۱ سیکل، به ۸ سیکل کلاک نیاز دارد. برای جبران این امر و رسیدن به **Throughput** مشابه محاسبات بیت‌موازی، در شتاب‌دهنده‌های بیت‌سریال تعداد واحدهای محاسباتی را بیشتر می‌کنند.

[توضیح: Latency یعنی زمان لازم برای تمام شدن یک عملیات. Throughput یعنی تعداد عملیات یا خروجی‌هایی که در واحد زمان تولید می‌شوند.]

### ۲. پیچیدگی کنترلر

معمولاً این شتاب‌دهنده‌ها نیاز به ماشین‌حالت‌های پیچیده‌تر هستند تا عملیات‌هایی از جمله استخراج بیت‌های ورودی و همگام‌سازی واحدهای محاسباتی را مدیریت کنند.

---

# بخش ۱: پیاده‌سازی پایه محاسبات سریال

در این گام باید معماری **Cluster**، واحد محاسباتی، و دیگر بخش‌های مورد نیاز را به‌گونه‌ای بازطراحی کنید که ورودی به‌صورت بیت‌سریال و وزن‌ها به‌صورت موازی پردازش شوند.

برای این کار لازم است که یک ماژول جدید طراحی کنید که ورودی را به‌صورت ۸ بیتی از حافظه خوانده، آن را ذخیره کند و در هر سیکل، یک بیت از آن را به همراه سیگنال‌های کنترلی لازم به تمام **PE**ها ارسال کند. این واحد، **Input Serializer** نام دارد.

[توضیح: در متن اصلی عبارت «واحد dnput serializer» آمده بود که به احتمال زیاد منظور Input Serializer است.]

در مرحله بعد، ضرب‌کننده‌های موازی پیشین باید با سخت‌افزار مناسب برای محاسبه بیت‌سریال، متشکل از گیت **AND** برای ضرب تک‌بیت ورودی در وزن، جمع‌کننده و عملگر شیفت، جایگزین شوند.

در طراحی دقت شود که در محاسبات، هر دو مقدار وزن و ورودی علامت‌دار می‌باشند.

[توضیح: علامت‌دار بودن ورودی و وزن مهم است، چون در نمایش مکمل دو، بیت علامت و عملیات شیفت باید درست مدیریت شوند. طراحی ساده unsigned برای signed جواب درست نمی‌دهد.]

همچنین با توجه به اینکه عملیات ضرب یک داده ۸ بیتی اکنون به ۸ سیکل کلاک زمان نیاز دارد، ماشین حالت در ماژول **cluster_ctrl** باید به‌روزرسانی شود تا داده‌های جدید، یعنی ورودی بعدی و وزن‌های متناظر، دقیقاً پس از اتمام این ۸ سیکل از حافظه واکشی شوند. همچنین سیگنال‌های کنترلی لازم باید به واحدهای محاسباتی، واحد **Input Serializer** و حافظه‌ها صادر شوند.

---

## موارد خواسته‌شده در این گام

### ۱. شماتیک مدارات طراحی‌شده

طراحی و رسم دقیق مسیر داده واحد محاسباتی بیت‌سریال و همچنین واحد کنترل ورودی **Input Serializer** که وظیفه پخش بیت‌به‌بیت ورودی را دارد، رسم کنید.

[توضیح: شماتیک باید نشان دهد ورودی ۸ بیتی چگونه ذخیره می‌شود، چگونه بیت‌ها یکی‌یکی انتخاب می‌شوند، مقدار shift چگونه تولید می‌شود و چگونه به PEها ارسال می‌شود.]

### ۲. طراحی کنترلر

اتوماتای **State machine** کنترلر جدید را رسم کنید. دقت کنید که **FSM** باید به‌گونه‌ای تغییر کند که پس از واکشی یک ورودی از حافظه، ۸ سیکل در یک حالت میانی بماند تا عملیات بیت‌سریال تکمیل شود و سپس آدرس بعدی را تولید کند.

[توضیح: برای پیاده‌سازی این رفتار معمولاً یک شمارنده ۳ بیتی برای شمارش ۰ تا ۷ لازم است.]

### ۳. کدهای سخت‌افزاری

کدهای **Verilog** مورد نیاز را نوشته و یا ماژول‌های پیشین را در صورت نیاز اصلاح کنید.

### ۴. تست واحد محاسباتی بیت‌سریال

یک **Testbench** جامع برای درستی‌سنجی واحد محاسباتی به‌تنهایی نوشته و نشان دهید که به ازای مجموعه‌ای از ورودی‌ها و وزن‌های مشخص، مقدار نهایی انباشت‌گرها در معماری بیت‌سریال دقیقاً با خروجی معماری موازی پایه تطابق دارد.

گزارش مقادیر مورد انتظار و شکل موج‌های مربوطه را ارائه دهید.

[توضیح: در این تست، بهتر است برای چند مقدار مثبت، منفی، صفر و مقادیر مرزی مثل 127 و -128 تست انجام شود؛ چون signed بودن داده‌ها می‌تواند باعث خطاهای پنهان شود.]

### ۵. تست کامل سیستم در Cocotb

با استفاده از شبیه‌ساز توسعه‌داده‌شده در بخش قبل، سیستم بیت‌سریال جدید را درستی‌سنجی کنید.

همچنین با اعمال یک لایه یکسان به‌عنوان ورودی به سیستم بیت‌سریال و بیت‌موازی، زمان اجرای آن را در دو حالت مقایسه کنید. برای مقایسه درست، تعداد واحدهای محاسباتی شتاب‌دهنده بیت‌سریال را ۸ برابر شتاب‌دهنده پایه در نظر بگیرید.

[توضیح: چون هر ضرب ۸ بیتی در بیت‌سریال ۸ سیکل طول می‌کشد، برای مقایسه منصفانه از نظر Throughput تعداد PEها در نسخه بیت‌سریال ۸ برابر در نظر گرفته می‌شود.]

---

# بخش ۲: بهینه‌سازی محاسبات، پرش از عملیات‌های بی‌اثر

با توجه به ماهیت داده‌ها در شبکه‌های عصبی، مخصوصاً پس از اعمال توابعی مانند **ReLU**، ورودی‌ها معمولاً دارای مقادیر صفر زیادی هستند، یا نمایش باینری آن‌ها شامل تعداد زیادی بیت صفر است. ضرب در صفر یک محاسبه بی‌اثر **Ineffectual Computation** است که صرفاً کلاک و توان مصرف می‌کند.

هدف این بخش، تبدیل تعداد کلاک‌های ثابت به کلاک‌های متغیر و وابسته به داده، یعنی **Data-dependent**، است. به این منظور، باید سیستم را به‌گونه‌ای تغییر دهید که تنها برای بیت‌های ورودی که مقدارشان ۱ است، سیکل کلاک مصرف کند و محاسبات بی‌اثر را به‌طور کامل **Skip** کند.

[توضیح: در حالت پایه بیت‌سریال، همیشه ۸ سیکل برای هر ورودی مصرف می‌شود؛ حتی اگر فقط یک بیت آن ۱ باشد. در نسخه بهینه، تعداد سیکل‌ها برابر تعداد بیت‌های ۱ ورودی است.]

---

## نیازمندی‌های طراحی

### ۱. استخراج موقعیت‌های غیرصفر

واحد کنترل ورودی، طراحی‌شده در گام قبل، را به‌گونه‌ای تغییر دهید که تنها بیت‌های دارای ارزش ۱ را پردازش کند. برای این کار باید از مدارهایی مانند **Leading-One Detector (LOD)** یا **Priority Encoder** استفاده کنید تا ورودی ۸ بیتی به مجموعه‌ای از اندیس‌ها، یعنی توان‌های ۲، که مقدارشان ۱ است تبدیل شود.

[توضیح: Priority Encoder در هر سیکل یکی از بیت‌های ۱ را پیدا می‌کند و موقعیت آن را به‌عنوان shift amount به PE می‌دهد. سپس آن بیت باید از ماسک ورودی حذف شود تا در سیکل بعدی بیت ۱ بعدی پیدا شود.]

### ۲. اصلاح مسیر داده در واحد محاسباتی

واحد **PE** اکنون به‌جای دریافت مقدار بیت ۰ یا ۱، «ارزش مکانی» یا **Shift Amount** بیت یک را از کنترلر دریافت می‌کند. PE باید وزن را به اندازه این ارزش مکانی شیفت داده و با انباشت‌گر جمع کند. با این روش، محاسبات بی‌اثر ضرب در بیت صفر به‌طور کلی skip می‌شوند.

[توضیح: در این حالت دیگر نیازی نیست PE برای بیت صفر کاری انجام دهد؛ چون فقط موقعیت بیت‌های ۱ برای PE ارسال می‌شود.]

### ۳. زمان‌بندی متغیر

در این حالت، زمان اجرای عملیات دیگر ثابت و برابر با ۸ سیکل نخواهد بود، بلکه زمان اجرا دقیقاً برابر با تعداد بیت‌های ۱، یعنی **Popcount**، در عدد ورودی است. کنترلر باید بتواند اتمام پردازش یک ورودی را تشخیص داده و بلافاصله واکشی داده بعدی را آغاز کند.

[توضیح: Popcount تعداد بیت‌های ۱ در نمایش دودویی عدد است. مثلاً popcount مقدار `00010110` برابر ۳ است.]

---

## موارد خواسته‌شده در این گام

### ۱. شماتیک مدار استخراج‌کننده

مداری طراحی کنید که در هر سیکل، موقعیت ارزش‌دارترین بیت ۱، یا کم‌ارزش‌ترین بیت ۱ بسته به طراحی شما، را در داده ورودی پیدا کند. برای این کار می‌توان از مدارهایی نظیر **Priority Encoder** یا **Leading-One Detector** استفاده کرد.

بلوک‌دیاگرام کلی سیستم را با در نظر گرفتن این مدار، واحدهای محاسباتی و سیگنال‌های میان آن‌ها رسم کنید.

در این بخش هم دقت کنید که ورودی و وزن هر دو علامت‌دار هستند و این موضوع را باید در طراحی لحاظ کنید.

همچنین توجه شود که مدار طراحی‌شده در هر سیکل کلاک باید قادر باشد موقعیت یک بیت غیرصفر را استخراج کند. برای مثال اگر ورودی دارای ۵ بیت غیرصفر است، موقعیت هر یک در یک سیکل استخراج شده و محاسبات در مجموعاً ۵ سیکل کامل می‌شود.

[توضیح: اگر از LOD استفاده شود، معمولاً از سمت بیت پرارزش‌تر شروع می‌کنید. اگر از Priority Encoder کم‌ارزش‌ترین بیت استفاده شود، از سمت LSB شروع می‌کنید. هر دو روش قابل قبول‌اند، به شرطی که مقدار شیفت درست به PE داده شود.]

[توضیح: برای داده‌های signed، باید مشخص شود نمایش بیت‌ها چگونه تفسیر می‌شود. اگر از مکمل دو استفاده می‌شود، بیت علامت وزن منفی دارد و پیاده‌سازی ساده جمع وزن شیفت‌داده‌شده ممکن است برای بیت علامت نیاز به اصلاح داشته باشد.]

### ۲. اصلاح مسیر داده در PE

در این حالت، **PE** به‌جای دریافت خود بیت، یعنی ۰ یا ۱، «ارزش مکانی» یا **Shift Amount** مربوط به آن بیت ۱ را دریافت می‌کند تا وزن را به همان اندازه شیفت داده و با انباشت‌گر جمع کند. شماتیک این تغییرات را ارائه دهید.

[توضیح: مسیر داده PE در این حالت معمولاً شامل رجیستر وزن، شیفتر، جمع‌کننده و انباشت‌گر است. ورودی اصلی PE دیگر bit نیست، بلکه shift amount است.]

### ۳. طراحی کنترلر زمان‌بندی متغیر

اتوماتا **State machine** کنترلر را به‌روز کنید. در این طراحی، زمان اجرای عملیات روی یک ورودی ۸ بیتی دیگر ثابت، یعنی ۸ سیکل، نیست؛ بلکه برابر با تعداد بیت‌های ۱ در آن ورودی است. کنترلر باید بتواند اتمام پردازش یک ورودی را تشخیص داده و بلافاصله کلمه ورودی بعدی را لود کند.

[توضیح: کنترلر می‌تواند یک ماسک از بیت‌های باقی‌مانده نگه دارد. هر بار که یک بیت ۱ پردازش شد، آن بیت از ماسک پاک می‌شود. وقتی ماسک صفر شد، پردازش ورودی تمام شده است.]

### ۴. تست واحد محاسباتی بیت‌سریال

یک **Testbench** همانند گام قبل برای درستی‌سنجی واحد محاسباتی به‌تنهایی نوشته و نشان دهید که به ازای مجموعه‌ای از ورودی‌ها و وزن‌های مشخص، مقدار نهایی انباشت‌گرها در معماری بیت‌سریال دقیقاً با خروجی معماری موازی پایه تطابق دارد.

گزارش مقادیر مورد انتظار و شکل موج‌های مربوطه را ارائه دهید.

[توضیح: بهتر است در این تست، علاوه بر بررسی مقدار نهایی، تعداد سیکل‌های مصرف‌شده نیز با popcount ورودی مقایسه شود.]

### ۵. تست کامل سیستم در Cocotb

با استفاده از شبیه‌ساز گام یک، همانند بخش قبل، سیستم بیت‌سریال جدید را نیز درستی‌سنجی کنید. با اعمال همان لایه بخش قبل به‌عنوان ورودی به نسخه جدید سیستم بیت‌سریال، زمان اجرای آن را با دو حالت پیشین مقایسه کنید.

[توضیح: سه حالت قابل مقایسه هستند: معماری بیت‌موازی پایه، معماری بیت‌سریال با ۸ سیکل ثابت، و معماری بیت‌سریال بهینه با زمان وابسته به داده.]